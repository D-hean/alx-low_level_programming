#include "main.h"
/**
 * _abs - returns absolute value of value
 * Return: absolute value
 * @n: the number evaluated
 */
int _abs(int n)
{
	if (n >= 0)
	{
	return (n);
	}
	else if (n < 0)
	return (-n);
}
